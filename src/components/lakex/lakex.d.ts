// Type declaration for the AUTO-CONVERTED ESM build of @alipay/lakex-doc
// (see ./lakex.js). The framework is shipped as a minified webpack bundle
// without type info, so we type its public surface loosely.
declare const Doc: {
  createOpenEditor: (container: HTMLElement, options: any) => any;
  [key: string]: any;
};

export default Doc;
